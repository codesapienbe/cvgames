# PLAYER

1. Run:
   ```bash
   python __init__.py
   ```

2. Play:
   - Pinch your index and middle fingers to "click" on-screen keys.
   - Type as shown on the virtual keyboard.

3. Quit: Press `q` 